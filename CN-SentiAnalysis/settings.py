# -*- coding: utf-8 -*-
import os

# 本项目的路径
basePath = os.path.dirname(os.path.realpath(__file__))

# 输入数据文件夹的路径
sourceData = f'{basePath}/srcData/'

# 标准文件夹的路径
staticDir = f'{basePath}/static/'

# 语料库的路径
corpusDir = f'{basePath}/corpus/'

# 输出数据文件夹的路径
dstData = f'{basePath}/dstData/'
